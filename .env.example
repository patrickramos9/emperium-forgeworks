# Frontend
VITE_APP_ENV=local
VITE_SITE_URL=http://localhost:5173
VITE_SITE_DOMAIN=emperiumforgeworks.com

# Optional — Plausible analytics (dashboard link + future storefront script)
VITE_PLAUSIBLE_DOMAIN=

# AWS (for Amplify sandbox CLI — never commit real values)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Stripe (optional — only when APP_ENV=deployment)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
